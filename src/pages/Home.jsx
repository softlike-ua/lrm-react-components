import React from 'react';
import { Button, Card } from '../components';

const Home = () => (
  <Card bgColor="red">
    content
    <Button bgColor="green" padding="10px">
      some text
    </Button>
  </Card>
);

export default Home;
